<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.upit</groupId>
		<artifactId>upit</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>

	<artifactId>upit-web-spa</artifactId>

	<packaging>war</packaging>
	<name>upit-web-spa</name>

	<dependencies>

		<dependency>
			<groupId>io.upit</groupId>
			<version>0.0.1-SNAPSHOT</version>
			<artifactId>upit-jpa-impl</artifactId>
		</dependency>

		<dependency>
			<groupId>io.upit</groupId>
			<version>0.0.1-SNAPSHOT</version>
			<artifactId>upit-jaxrs-bindings</artifactId>
		</dependency>

		<!-- Third party deps -->

		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>servlet-api</artifactId>
			<version>2.5</version>
		</dependency>

		<dependency>
			<groupId>com.google.inject.extensions</groupId>
			<artifactId>guice-servlet</artifactId>
			<version>3.0</version>
		</dependency>

	</dependencies>

	<build>
		<plugins>

			<plugin>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-maven-plugin</artifactId>
				<version>9.1.1.v20140108</version>
			</plugin>

			<plugin>
				<groupId>org.lesscss</groupId>
				<artifactId>lesscss-maven-plugin</artifactId>
				<version>1.3.3</version>
				<configuration>
					<sourceDirectory>${project.basedir}/src/main/webapp/css</sourceDirectory>
					<outputDirectory>${project.build.directory}/${project.build.finalName}/css</outputDirectory>

					<compress>true</compress>
					<includes>
						<include>upit.less</include>
					</includes>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>compile</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>io.upit.tools</groupId>
				<artifactId>jaxrs-js-gen</artifactId>
				<version>0.0.1-SNAPSHOT</version>

				<configuration>
					<outputDirectory>target/generated/</outputDirectory>
					
					<!-- Classes to generate from -->
					<resourceClasses>
						<resourceClass>io.upit.jaxrs.resources.UserResource</resourceClass>
						<resourceClass>io.upit.jaxrs.resources.AuthSessionResource</resourceClass>
					</resourceClasses>
				</configuration>
				
				<executions>
					<execution>
						<phase>generate-sources</phase>
						<goals>
							<goal>generate</goal>
						</goals>
					</execution>
				</executions>
				
				<dependencies>
					<!-- The code generator plugin needs build-time access to upit-core-jpa 
						so we must list it explicitly as a plugin dependency -->
					<dependency>
						<groupId>io.upit</groupId>
						<version>0.0.1-SNAPSHOT</version>
						<artifactId>upit-jaxrs-bindings</artifactId>
					</dependency>
				</dependencies>
			</plugin>

			<plugin>
				<groupId>com.github.bringking</groupId>
				<artifactId>requirejs-maven-plugin</artifactId>
				<version>2.0.2</version>
				<executions>
					<execution>
						<goals>
							<goal>optimize</goal>
						</goals>
						<phase>none</phase>
					</execution>
				</executions>

				<configuration>
					<!-- optional path to a nodejs executable -->
					<!-- <nodeExecutable> /opt/nodejs/node </nodeExecutable> -->
					<!-- path to optimizer json config file -->

					<configFile>src/main/config/build.js</configFile>

					<!-- optional parameters to optimizer executable -->
					<!-- <optimizerParameters> <parameter>optimize=uglify</parameter> <parameter>baseUrl=${baseDir}</parameter> 
						</optimizerParameters> -->

					<!-- Whether or not to process configFile with maven filters. If you 
						use this option, some options in your configFile must resolve to absolute 
						paths (see below) -->
					<filterConfig>
						false
					</filterConfig>
					<!-- Skip requirejs optimization if true -->
					<skip>
						false
					</skip>
				</configuration>
			</plugin>

		</plugins>

	</build>

</project>
